import 'package:flutter/material.dart';

class VoucherBalancePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Voucher Balance"),
      ),
      body: Center(
        child: Text("This is the Voucher Balance Page"),
      ),
    );
  }
}